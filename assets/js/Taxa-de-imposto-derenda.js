let line = Number(gets());
console.log(line);

/*function calcularImposto(valor){
    let valorTotal = 0;
    if(valor >= 0 && valor <= 200.00){
        return "Insento";
    }else if(valor >= 2000.01 && valor <= 3000.00){
        valorTotal =  valor - (valor * 0.08);
    }else if(valor >= 3000.01 && valor <= 4500.00){
        valorTotal = valor - (valor * 0.18);
    }else{
        valorTotal = valor - (valor * 0.28);
    }
    return "R$ " + valorTotal;
}*/